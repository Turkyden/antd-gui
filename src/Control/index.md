## Control

可拖拽折叠 GUI 面板控件

```tsx
/**
 * transform: true
 */
import React from 'react';
import { Control } from 'antd-gui';

export default () => (
  <div className="h-96">
    <Control title="💜 Readme Card" defaultPosition={{ x: -16, y: 16 }}>
      bala bala ...
    </Control>
  </div>
);
```
